model Customer {
  id             String   @id @default(cuid())
  name           String   // Ticari Ünvan
  contactPerson  String   // Yetkili
  taxOffice      String?
  taxOrEmail     String   // Vergi No veya Email
  phone          String?

  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  transactions   Transaction[]
}