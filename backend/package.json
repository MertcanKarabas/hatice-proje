{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "prisma:migrate": "npm run prisma:combine && prisma migrate dev",
    "prisma:studio": "npm run prisma:combine && prisma studio",
    "build": "npm run prisma:generate && nest build && tsc-alias",
    "format": "prettier --write 'src/**/*.ts' 'test/**/*.ts'",
    "start": "nest start",
    "start:debug": "nest start --debug --watch",
    "start:prod": "node dist/main",
    "lint": "eslint '{src,apps,libs,test}/**/*.ts'",
    "lint:fix": "eslint '{src,apps,libs,test}/**/*.ts' --fix",
    "test": "jest",
    "prisma:generate": "npm run prisma:combine && prisma generate",
    "prisma:combine": "mkdir -p prisma && cat prisma/parts/*.prisma > prisma/schema.prisma",
    "start:dev": "npm run prisma:generate && nest start --watch",
    "postinstall": "npm run prisma:generate"
  },
  "dependencies": {
    "@nestjs/axios": "^4.0.1",
    "@nestjs/common": "^11.1.4",
    "@nestjs/config": "^4.0.2",
    "@nestjs/core": "^11.1.4",
    "@nestjs/jwt": "^11.0.0",
    "@nestjs/mapped-types": "^2.1.0",
    "@nestjs/passport": "^11.0.5",
    "@nestjs/platform-express": "^11.1.4",
    "@nestjs/throttler": "^6.4.0",
    "@prisma/client": "^6.14.0",
    "@types/helmet": "^0.0.48",
    "axios": "^1.11.0",
    "bcrypt": "^6.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "eslint": "^9.32.0",
    "helmet": "^8.1.0",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.7",
    "@nestjs/schematics": "^11.0.5",
    "@nestjs/testing": "^11.1.4",
    "@types/bcrypt": "^5.0.2",
    "@types/jest": "^30.0.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.0.14",
    "@types/passport-jwt": "^4.0.1",
    "@typescript-eslint/eslint-plugin": "^8.38.0",
    "eslint-config-prettier": "^10.1.8",
    "jest": "^30.0.4",
    "prisma": "^6.14.0",
    "ts-jest": "^29.4.0",
    "typescript": "^5.8.3"
  }
}
